* re-work configuration (e.g. pop3 server port in jar file name), or at least externalize conf with a set of default built-in
	-> avoid to re-compile for a simple configuration change!
* POP3 bug: sometime downloaded emails are marked as read
* POP3 bug: sometime deleting a message does not delete it (it is re-fetched on next mail check)
* POP3 bug: downloaded emails have an extra "." at the end
	-> Thunderbird bug!
* POP3: additional commands (logger, etc.)
* POP3: TOP command is incomplete (only TOP n 0 is supported)
* POP3: DELE, LIST, RETR, UIDL: argument may NOT refer to a message marked as deleted
* POP3: allow to bind to a specific interface
* POP3: limit the number of simultaneous connections
* utility to list (IMAP) folders and their uid
* MBOX: mboxcl and mboxcl2 export (fr.cedrik.inotes.mbox)
* MBOX: maildir (and maildir++ once we can fetch iNotes folders) export
* calendar (CalDAV)
* contacts (CardDAV)
* IMAP4rev1 (rfc 3501)
