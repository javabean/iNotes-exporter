* POP3 bug: downloaded emails have an extra "." at the end
* POP3 bug: downloaded emails are marked as read
* POP3 bug: sometime deleting a message does not delete it (it is re-fetched on next mail check)
* main to dispatch between mbox and pop3
* POP3: append X-iNotes- headers?
* POP3: additional commands (quota size, logger, etc.)
* POP3: TOP command is incomplete (only TOP n 0 is supported)
* POP3: DELE, LIST, RETR, UIDL: argument may NOT refer to a message marked as deleted
* POP3: allow to bind to a specific interface
* utility to list (IMAP) folders and their uid
* mboxcl and mboxcl2 export (fr.cedrik.inotes.mbox)
* maildir (and maildir++ once we can fetch iNotes folders) export
* re-work configuration (e.g. pop3 server port in jar file name), or at lease externalize conf with a set of default built-in
* calendar
* contacts
* IMAP4rev1 (rfc 3501)
